from transformers import pipeline

# class FacebookBartMNLIPipLine():
#     def __init__(self, config, device=None):
#         self.pipeline = pipeline("zero-shot-classification", model=config.predict.predict.model_path,
#                                  device=device)
#
#     def predict(self, feed_dict):
#         return self.pipeline()